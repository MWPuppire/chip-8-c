#include <shared.h>
#include <memory.h>
#include <registers.h>
#include <instructions.h>

int keyEquals(struct emuState *UNUSED(state), UWord UNUSED(word)) {
	return 0;
}

int keyNotEquals(struct emuState *UNUSED(state), UWord UNUSED(word)) {
	return 0;
}

int getKey(struct emuState *UNUSED(state), UWord UNUSED(word)) {
	return 0;
}
